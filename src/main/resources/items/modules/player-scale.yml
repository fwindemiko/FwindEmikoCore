# 玩家缩放模块配置
# 功能：临时改变玩家碰撞箱大小
# 可被其他物品模块调用

# CraftEngine 物品命名空间ID（可选，用于独立物品触发）
item-id: "miragedge_items:scale_changer"

# 是否启用独立物品触发模式
item-trigger-enabled: false

# 默认缩放比例 (1.0 = 正常, 0.5 = 一半, 2.0 = 两倍)
default-scale: 0.5

# 默认持续时间（秒）
default-duration: 10

# 默认冷却时间（秒）
default-cooldown: 30
